name: Build ShadowMaster Combined

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Create Independent ShadowUI.m
        run: |
          cat <<EOF > "ShadowUI.m"
          #import <Foundation/Foundation.h>
          #import <UIKit/UIKit.h>

          @interface ShadowUI : NSObject
          + (void)alert:(NSString *)title msg:(NSString *)msg;
          @end

          @implementation ShadowUI
          + (void)alert:(NSString *)title msg:(NSString *)msg {
              dispatch_async(dispatch_get_main_queue(), ^{
                  UIWindow *window = nil;
                  if (@available(iOS 13.0, *)) {
                      for (UIWindowScene* scene in [UIApplication sharedApplication].connectedScenes) {
                          if (scene.activationState == UISceneActivationStateForegroundActive) {
                              window = scene.windows.firstObject;
                              break;
                          }
                      }
                  }
                  if (!window) window = [UIApplication sharedApplication].keyWindow;

                  UIAlertController *a = [UIAlertController alertControllerWithTitle:title 
                                           message:msg 
                                           preferredStyle:UIAlertControllerStyleAlert];
                  [a addAction:[UIAlertAction actionWithTitle:@"OK" style:UIAlertActionStyleDefault handler:nil]];
                  
                  UIViewController *root = window.rootViewController;
                  while (root && root.presentedViewController) root = root.presentedViewController;
                  [root presentViewController:a animated:YES completion:nil];
              });
          }
          @end

          __attribute__((constructor))
          static void shadow_ui_init() {
              dispatch_after(dispatch_time(DISPATCH_TIME_NOW, 12 * NSEC_PER_SEC), dispatch_get_main_queue(), ^{
                  [ShadowUI alert:@"ShadowMaster v11" msg:@"✅ UI & Protection Loaded Successfully"];
              });
          }
          EOF

      - name: Clean Original File From Duplicates
        run: |
          # هذا السطر سيقوم بحذف أي سطر يحتوي على @implementation أو @interface أضفناه سابقاً بالخطأ
          # لترك الملف الأصلي نظيفاً للمترجم
          sed -i '' '/@implementation MemoryExploiter/d' "SHADOW MASTER v11.m"
          sed -i '' '/@implementation BehaviorSpoofer/d' "SHADOW MASTER v11.m"
          sed -i '' '/@implementation ShadowMasterCore/d' "SHADOW MASTER v11.m"
          
          # إضافة الاستيرادات الضرورية فقط في أول سطر
          sed -i '' '1i\
          #import <Foundation/Foundation.h>\
          #import <UIKit/UIKit.h>\
          #import <CoreML/CoreML.h>\
          ' "SHADOW MASTER v11.m"

      - name: Generate Project
        run: xcodegen generate

      - name: Build Final Dylib
        run: |
          mkdir -p output
          xcodebuild -project ShadowMasterProject.xcodeproj \
          -scheme ShadowMaster \
          -configuration Release \
          -sdk iphoneos \
          CONFIGURATION_BUILD_DIR=$(pwd)/output \
          clean build \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGNING_REQUIRED=NO \
          EAGER_LINKING_TBD_GENERATION_ENABLE=NO \
          OTHER_CFLAGS="-Wno-everything -D'interface=class'"

      - name: Upload Result
        uses: actions/upload-artifact@v4
        with:
          name: ShadowMaster-Dylib
          path: output/ShadowMaster.dylib
