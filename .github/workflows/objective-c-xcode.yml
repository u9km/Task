name: Build ShadowMaster Combined (Stable)

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v4

      - name: 🛠️ Smart Patching (Fix Syntax & Logic Errors)
        run: |
          echo "🔧 Creating Header for Missing Types..."
          # إنشاء ملف ترويسة يحتوي على كافة التعريفات المفقودة لضمان نجاح الربط
          cat << 'EOF' > ShadowDefinitions.h
          #import <Foundation/Foundation.h>
          #import <UIKit/UIKit.h>
          #import <CoreML/CoreML.h>

          // تعريف الهياكل (Structs) المطلوبة في دوال السلوك والفيزياء
          typedef struct { float x, y, z; } PlayerData;
          typedef struct { float x, y; } AimData;
          typedef struct { float x, y, z; } MovementData;
          typedef struct { float fov; } VisionData;
          typedef struct { float gravity; } PhysicsData;
          typedef struct { float max_speed; } MoveConstraints;
          typedef struct { int count; } ShotData;
          
          // تعريف الفئات (Classes) المفقودة لمنع أخطاء 'unknown type'
          @class MLModel, VideoFrame, CheatPrediction, ClientState, ValidationResult;
          @class PlayerAction, CheatDetection, SecurityAlert, VulnerabilityAssessment;
          @class VulnerabilityAnalysis, AttackPlan, AttackerDashboard;

          // تعريف واجهة AttackerDashboard لضمان نجاح استدعاء [AttackerDashboard shared]
          @interface AttackerDashboard : NSObject
          + (instancetype)shared;
          - (void)updateWithVulnerability:(id)vuln;
          + (void)launch;
          @end
          EOF

          echo "🩹 Patching SHADOW MASTER v11.m..."
          # 1. إزالة أي تعريفات مكررة في الأسطر الأولى (تسبب خطأ duplicate interface)
          sed -i '' '/@interface.*NSObject @end/d' "SHADOW MASTER v11.m" || true
          
          # 2. حقن ملف التعريفات في بداية الملف
          sed -i '' '1i\
          #import "ShadowDefinitions.h"\
          ' "SHADOW MASTER v11.m" || true

          echo "🩹 Patching ShadowUI.m..."
          # إصلاح تحذير keyWindow القديم ليتوافق مع iOS 13+
          sed -i '' 's/\[UIApplication sharedApplication\]\.keyWindow/\[\[\[UIApplication sharedApplication\] windows\] firstObject\]/g' ShadowUI.m || true

      - name: 🚀 Build Combined Dylib
        run: |
          mkdir -p output
          SDK_PATH=$(xcrun --sdk iphoneos --show-sdk-path)

          echo "Building for iOS arm64..."
          clang++ -arch arm64 \
            -dynamiclib \
            -std=c++17 \
            -isysroot "$SDK_PATH" \
            -miphoneos-version-min=14.0 \
            -O3 \
            -fobjc-arc \
            -w \
            -fpermissive \
            -I. \
            "SHADOW MASTER v11.m" \
            ShadowUI.m \
            -framework Foundation \
            -framework UIKit \
            -framework Security \
            -framework CoreGraphics \
            -framework CoreImage \
            -framework CoreML \
            -framework QuartzCore \
            -framework Metal \
            -framework MetalKit \
            -lc++ \
            -ldl \
            -o output/ShadowMaster.dylib

      - name: 🛡️ Strip & Verify
        run: |
          xcrun strip -x output/ShadowMaster.dylib
          file output/ShadowMaster.dylib
          ls -lh output/

      - name: 📦 Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ShadowMaster_Final
          path: output/ShadowMaster.dylib
